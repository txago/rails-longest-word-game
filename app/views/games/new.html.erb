<h1>Word Game</h1>
<p>What's the longest word you can find with the letter below?</p>

<div style="display: flex;">
  <% @letters.each do |letter| %>
    <div style="background-color: #333; color: white; width: 40px; height: 40px; padding: 10px; margin: 10px 10px 10px 0; display: flex; align-items: center; justify-content: center;"><%= letter.upcase %></div>
    <% end %>
</div>

<div>
  <form action="/score" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :letters, @letters %>
    <input type="text" name="word" placeholder="Write a word">
    <input type="submit" value="Play!">
  </form>
</div>
